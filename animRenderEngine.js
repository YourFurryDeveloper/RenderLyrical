let animStylesheet = "";
let animJS = "";

function playAnim() {
    Object.keys(projJsonFile).forEach(sceneObject => {
        let newCssKeyframeDef = `
        @keyframes objectAnim_${sceneObject} {
            
        }`
    })
}